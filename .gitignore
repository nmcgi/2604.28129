# Python virtual environment
.venv/
venv/
env/

# Python cache
__pycache__/
*.py[cod]
*.pyo

# Large / externally-sourced data — reproducible, not committed
data/lmsys/
data/safedial/
data/activations/
data/synthetic_extended/
# data/synthetic/ is intentionally committed (starter dataset for smoke check)

# Trained model artefacts — large, reproducible via train_probe.py
models/

# uv lock file — pin for reproducibility; keep in repo
# uv.lock

# Original paper PDF — removed from tracking; link in README instead
2604.28129v1.pdf

# OS noise
.DS_Store
Thumbs.db

# env vars
.env*
